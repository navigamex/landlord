spring:
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
      username: nacos
      password: nacos
      config:
        namespace: dev
        file-extension: yml
      discovery:
        namespace: dev
  application:
    name: game-service  # 当前服务名
server:
  port: 10005